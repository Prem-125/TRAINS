class Mailsystem:
    def __init__(mailboxCount):
        mailboxes = new arraylist

        for(int i= 0; i<mailboxCount; i++):
            passcode = "" + i+1
            greeting = "You reached mailbox. Leave a message now."
            mailboxes.add(Mailbox(passcode, greeting))
    
    def findMailbox(ext)
    int i = parseInt